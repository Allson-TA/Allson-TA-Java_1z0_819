package p118;

public class p118 {
	/*
	1. List fruits = List.of("banana", "orange", "apple", "lemon");
	2. fruits.sort(new Comparator() {
	     @Override
	     public int compare(String m, String n) {
	         return n.compareTo(m);
	     }
	 });

	Which statement will refactor line 2 to use a lambda expression?
	哪個語句會將第2行重構為使用lambda表達式？

	A. fruits.sort((String x, y) -> {return y.compareTo(x);});
	B. fruits.sort((a, b) -> {return b.compareTo(a);});
	C. fruits.sort((String d, String e) -> {e.compareTo(d);});
	D. fruits.sort( o, p -> p.compareTo(o));
	*/

}
