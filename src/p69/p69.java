package p69;

public class p69 {
	/*
	Examine this excerpt from the declaration of the java.se module:
	檢查來自 java.se 模組宣告的這段摘錄：

	module java.se {
	    ...
	    requires transitive java.xml
	    ...
	}

	What does the transitive modifier mean?
	transitive 修飾符意味著什麼？

	A. Any module that attempts to require the java.se module actually requires the java.xml module.
	   任何嘗試需要 java.se 模組的模組實際上需要 java.xml 模組。

	B. Any module that requires the java.xml module does not need to require the java.se module.
	   任何需要 java.xml 模組的模組不需要需要 java.se 模組。

	C. Only a module that requires the java.se module is permitted to require the java.xml module.
	   只有需要 java.se 模組的模組才被允許需要 java.xml 模組。

	D. Any module that requires the java.se module does not need to require the java.xml module.
	   任何需要 java.se 模組的模組不需要需要 java.xml 模組。
	*/

}
