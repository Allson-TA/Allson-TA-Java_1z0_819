package p110;

public class p110 {
	/*
	Your organization provides a cloud server to your customer to run their Java code. You are reviewing the changes for the next release and you see this change in one of the config files:
	您的組織為客戶提供雲伺服器以運行他們的 Java 代碼。 您正在審查下一版本的更改，並且在其中一個配置文件中看到了這一更改：

	old: JAVA_OPTS="$JAVA_OPTS -Xms8g -Xmx8g"
	new: JAVA_OPTS="$JAVA_OPTS -Xms8g -Xmx8g -noverify"

	Which is correct ?
	哪一個是正確的？

	A. You accept the change because -noverify is necessary for your code to run with the latest version of Java.
	   您接受更改，因為要使用最新版本的 Java 運行代碼，必須使用 -noverify。

	B. You accept the change because -noverify is a standard option that has been supported since Java 1.0.
	   您接受更改，因為 -noverify 是自 Java 1.0 以來支持的標準選項。

	C. You reject the change because -noverify is critical security risk.
	   您拒絕更改，因為 -noverify 是嚴重的安全風險。

	D. You reject the change because -Xms8g -Xmx8g uses too much system memory.
	   您拒絕更改，因為 -Xms8g -Xmx8g 使用了太多的系統內存。
	*/

}
