package p89;

public class p89 {
	
	/*
	Which two are valid statements?
	哪兩個是有效的語句？
	
	A. BiPredicate<Integer, Integer> test = (Integer x, final Integer y) -> (x.equals(y));
	B. BiPredicate<Integer, Integer> test = (final var x, y) -> (x.equals(y));
	C. BiPredicate<Integer, Integer> test = (Integer x, final var y) -> (x.equals(y));
	D. BiPredicate<Integer, Integer> test = (final Integer x, var y) -> (x.equals(y));
	E. BiPredicate<Integer, Integer> test = (var x, final var y) -> (x.equals(y));
	
	 */

}
